# تشغيل وإدارة ParseFlow (منتج Disha Pro Studio)

## أسهل طريقة للإدارة

استخدم Sites من ChatGPT لتعديل الموقع واختبار التغييرات ونشرها. لا تحتاج إلى Termux ولا إلى GitHub لتشغيل الموقع المنشور. حدد التعديل المطلوب واطلب تطبيقه على الموقع الحالي. يمكن إدارة اسم الموقع، الرابط الفرعي، الوصول العام أو الخاص، والنطاق المخصص من إمكانات Sites المتاحة لحسابك. تغيير النطاق المخصص يحتاج إلى امتلاك النطاق وضبط DNS.

إعدادات الاستخدام داخل الصفحة: ترميز النص، تفعيل OCR، اسم الملف وصيغة التصدير، وحفظ الجلسة أو استرجاعها. لا توجد لوحة مدير مستقلة أو قاعدة بيانات لمستندات المستخدمين. النصوص والملفات تعالج داخل المتصفح؛ إغلاق الصفحة يمسح الجلسة ما لم تحفظها بنفسك. ملف الجلسة غير مشفر.

## متطلبات استخدام الموقع

متصفح حديث يدعم JavaScript وWeb Crypto وWeb Workers. لا يحتاج المستخدم إلى Python أو Node.js. يلزم الإنترنت لفتح التطبيق وتحميل مكتبات القراءة، وخصوصًا نماذج OCR العربية والإنجليزية عند الاستخدام الأول. الموقع ليس تطبيقًا مضمون العمل دون إنترنت.

الحدود الحالية: 20 MB لكل ملف، 10 ملفات في الدفعة، مليونا حرف للنص، 200 صفحة PDF نصي، أو 20 صفحة مع OCR. القراءة والتصدير يستخرجان النص ولا يحافظان على تصميم المستند. ترتيب PDF المركب ونتائج OCR يحتاجان إلى مراجعة بشرية.

## تشغيل محلي اختياري

المشروع TypeScript/React باستخدام Vinext وVite، ويستهدف Cloudflare Workers عند النشر عبر Sites. ليس سكريبت Python؛ بيئة venv لا تشغله.

المتطلبات: Node.js إصدار 22.13 أو أحدث، وpnpm بالإصدار المسجل في package.json (حاليًا 11.25.0)، وإنترنت لتثبيت الاعتماديات أول مرة. استخدم نسخة حديثة متوافقة من Node.js. على Linux يلزم bash وflock وGNU timeout لسكريبت التثبيت المرفق.

داخل مجلد المشروع بعد إعداد Node.js وpnpm:

```bash
pnpm run install:ci
pnpm run dev
```

افتح العنوان الذي يظهر في الطرفية. لفحص الأنواع وبناء نسخة النشر:

```bash
pnpm exec tsc --noEmit
pnpm run lint
pnpm run build
```

الكمبيوتر بنظام Linux هو المسار الأنسب للتطوير المحلي. Ubuntu داخل Termux قد يواجه عدم توافق في workerd أو المكتبات الثنائية على Android/proot؛ لم تُختبر هذه البيئة لهذا الإصدار. لا يلزم تشغيل السيرفر على هاتفك لبقاء الموقع المنشور متاحًا.

## أماكن الإعدادات في الكود

- app/page.tsx: الواجهة وخيارات الأدوات ورسائل الاستخدام.
- app/globals.css: الألوان والتنسيق والتجاوب.
- lib/limits.ts: حدود قراءة الملفات وعدد صفحات PDF وOCR.
- lib/readers/: قارئات النص وPDF وOffice وOCR.
- lib/text-tools.ts: معالجة النصوص والإحصائيات.
- lib/export.ts وlib/crypto.ts: التصدير والتشفير.
- package.json وpnpm-lock.yaml: الاعتماديات وأوامر التشغيل.
- .openai/hosting.json: هوية الموقع وإعدادات الربط المدعومة؛ لا تضع أسرارًا فيه.

عند تغيير الحدود، حدّث كذلك التحقق ورسائل الواجهة في app/page.tsx. لا تضع كلمات سر أو مفاتيح API في ملفات المتصفح. هذا الإصدار لا يحتاج مفتاح ذكاء اصطناعي أو اشتراك API لقراءة الملفات أو OCR.

## الاختبارات

scripts/browser-checks.ts يحتوي اختبارات تكامل تعمل داخل المتصفح، لتشغيلها محليًا: أنشئ مجلدي app/qa وpublic/qa، وانسخ scripts/qa-page.tsx.txt إلى app/qa/page.tsx وscripts/fixtures/sample.pdf إلى public/qa/sample.pdf، ثم افتح /qa واضغط Run checks. احذف app/qa وpublic/qa بعد الانتهاء وقبل البناء والنشر. لا تُنشر صفحة الاختبار ضمن الموقع. scripts/readers.test.mjs المرفق الأصلي يحتاج إلى بيئة DOM ودعم استيراد TypeScript؛ تشغيله مباشرة في Node دون تهيئة لا يكفي.

تحديث المصدر وحده لا يحدّث الموقع الحي؛ يلزم بناء نسخة ناجحة ثم نشرها عبر Sites. GitHub اختياري لحفظ نسخة أخرى من الكود ولا ينشر هذا الموقع تلقائيًا.
