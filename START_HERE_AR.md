# ابدأ هنا — تسليم Disha Pro Studio للتطوير

هذه نسخة كاملة من مصدر الإصدار المنشور رقم 3، وليست حزمة التعديلات الجزئية.
الموقع: https://disha-pro-studio.amgroblica.chatgpt.site

## للذكاء الاصطناعي أو المطوّر المستلم

ابدأ بقراءة هذا الملف ثم MANAGEMENT_AR.md وTEST_RESULTS.md وpackage.json وapp/page.tsx وlib/processor.ts.
حافظ على وظائف التطبيق الحالية وواجهة العربية RTL. افحص التعديل المطلوب، ثم نفّذه واختبره قبل اقتراح النشر. سلّم المشروع كاملًا مع ملخص الملفات المعدلة، وليس ملفات ترقيع ناقصة. لا تنشر تعديلًا على الموقع دون طلب صاحبه.

## محتويات الحزمة

- جميع ملفات المصدر المتتبعة في النسخة المنشورة، بما فيها الواجهة، قارئات الملفات، التصدير والتشفير، مكونات الواجهة والتنسيق.
- public/: الأصول، PDF.js worker، خرائط الأحرف والخطوط وتراخيصها.
- package.json وpnpm-lock.yaml وpnpm-workspace.yaml: قائمة الاعتماديات وإصداراتها وإعدادات تثبيتها.
- scripts/: التثبيت والبناء واختبارات التكامل وملف PDF تجريبي وقالب صفحة الاختبارات.
- dist/: ناتج البناء الذي استُخدم للإصدار الحالي؛ أعد بناءه بعد أي تعديل ولا تطوره يدويًا.
- reference/disha-pro.py: سكريبت Python الأصلي المرجعي؛ التطبيق الحالي TypeScript وليس Python.
- إعدادات Sites والبناء وTypeScript، وأمثلة قاعدة البيانات الموجودة في القالب.
- EXPORT_MANIFEST.json: مرجع الإصدار وبصمة SHA-256 لكل ملف للتحقق من اكتمال النسخة.

## التشغيل

يلزم Node.js 22.13 أو أحدث وpnpm 11.25.0. راجع README.md وMANAGEMENT_AR.md للتفاصيل.
بعد فك الضغط افتح الطرفية داخل مجلد disha-pro-studio ثم:

```bash
pnpm install --frozen-lockfile
pnpm run dev
```

لفحص المشروع وبنائه:

```bash
pnpm exec tsc --noEmit
pnpm run lint
pnpm run build
```

اختبارات المتصفح: اتبع تعليمات MANAGEMENT_AR.md لإضافة صفحة QA المؤقتة وملفها التجريبي، ثم احذفهما قبل النشر.

## حدود النسخة

لا يتضمن ZIP مجلد node_modules أو ذاكرة التخزين المؤقت أو سجل Git؛ الاعتماديات تُثبّت من ملف القفل. يلزم الإنترنت لتثبيتها ولتحميل نماذج OCR عند أول استخدام، ولذلك هذه ليست حزمة تشغيل مستقلة دون إنترنت.
لا توجد قاعدة بيانات مستخدمين أو مخزن ملفات متصلان بهذا الإصدار (d1 وr2 غير مفعّلين). الملفات والنصوص والجلسات تُعالج في متصفح المستخدم؛ الملفات التي نزلها المستخدم على هاتفه لا توجد على خادم الموقع ولا تدخل في هذا التصدير.
لا توجد كلمات سر أو رموز دخول للاستضافة داخل الحزمة. هوية مشروع Sites موجودة في إعداداته لكنها ليست تصريح وصول. النشر على استضافة أخرى قد يحتاج تكييف تكامل Sites وCloudflare؛ ليس مجرد رفع ZIP لاستضافة ملفات ثابتة.

## حالة الاختبارات

نجح في جلسة التحديث: 28 اختبار تكامل بالمتصفح، وفحص TypeScript وESLint والبناء النهائي، وتجربة التنظيف والتراجع في الواجهة. راجع TEST_RESULTS.md للحدود: لا يعني ذلك ضمان دقة كل مستند أو OCR أو اختبار Android/proot. بعض الدوال الجديدة موجودة في lib/text-tools.ts لكنها غير موصولة بأزرار الواجهة الحالية.

هذا التصدير لم يعدّل الموقع المنشور.
